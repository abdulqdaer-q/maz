import { Countries } from "@/types/Country";
import { Option } from "@/types/Option";
import { axios } from "@/utils/axios";
import React, { useEffect, useState } from "react";

export default (createDate: string) => {
    const [daysDiff] = useState<number>();

    return daysDiff;
};
